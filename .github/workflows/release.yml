name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  windows-release:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate release version
        if: startsWith(github.ref, 'refs/tags/')
        shell: pwsh
        run: |
          $tagVersion = "${{ github.ref_name }}" -replace '^v', ''
          $cmake = Get-Content CMakeLists.txt -Raw
          $match = [regex]::Match($cmake, 'project\(ZStreamEye\s+VERSION\s+([0-9]+(?:\.[0-9]+){1,3})')
          if (-not $match.Success) {
            throw "Could not read project version from CMakeLists.txt"
          }
          $projectVersion = $match.Groups[1].Value
          if ($tagVersion -ne $projectVersion) {
            throw "Release tag version ($tagVersion) must match CMake project version ($projectVersion)"
          }
          $releaseNotes = "docs/releases/${{ github.ref_name }}.md"
          if (-not (Test-Path $releaseNotes)) {
            throw "Release notes file is required: $releaseNotes"
          }

      - name: Set up MSYS2 UCRT64
        id: msys2
        uses: msys2/setup-msys2@v2
        with:
          msystem: UCRT64
          update: true
          install: >-
            mingw-w64-ucrt-x86_64-gcc
            mingw-w64-ucrt-x86_64-cmake
            mingw-w64-ucrt-x86_64-ninja
            mingw-w64-ucrt-x86_64-pkgconf
            mingw-w64-ucrt-x86_64-qt6-base
            mingw-w64-ucrt-x86_64-qt6-tools
            mingw-w64-ucrt-x86_64-ffmpeg

      - name: Configure
        shell: msys2 {0}
        run: >
          cmake -S . -B build-msys2-ucrt -G Ninja
          -DCMAKE_BUILD_TYPE=Release
          -DCMAKE_PREFIX_PATH=/ucrt64
          -DBUILD_TESTING=ON

      - name: Build
        shell: msys2 {0}
        run: cmake --build build-msys2-ucrt

      - name: Test
        shell: msys2 {0}
        run: ctest --test-dir build-msys2-ucrt --output-on-failure

      - name: Install Inno Setup
        shell: pwsh
        run: choco install innosetup --no-progress -y

      - name: Package
        shell: pwsh
        env:
          MSYS2_LOCATION: ${{ steps.msys2.outputs.msys2-location }}
        run: |
          ./scripts/deploy-windows-msys2.ps1
          $version = "${{ github.ref_name }}"
          if ([string]::IsNullOrWhiteSpace($version) -or $version -eq "main") {
            $version = "manual-${{ github.run_number }}"
          }
          ./scripts/package-windows-installer.ps1 -Version $version
          Move-Item `
            -LiteralPath dist/ZStreamEye-windows-ucrt64.zip `
            -Destination "dist/ZStreamEye-$version-windows-ucrt64.zip" `
            -Force

      - name: Upload release artifact
        uses: actions/upload-artifact@v4
        with:
          name: ZStreamEye-${{ github.ref_name }}-windows-ucrt64
          path: |
            dist/ZStreamEye-*-windows-ucrt64.zip
            dist/ZStreamEye-*-windows-ucrt64-setup.exe
          retention-days: 30

      - name: Publish GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          body_path: docs/releases/${{ github.ref_name }}.md
          fail_on_unmatched_files: true
          make_latest: true
          files: |
            dist/ZStreamEye-*-windows-ucrt64.zip
            dist/ZStreamEye-*-windows-ucrt64-setup.exe
